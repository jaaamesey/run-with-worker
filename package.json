{
  "name": "run-in-worker",
  "version": "0.0.1",
  "description": "Easily run inline functions inside an ephemeral web worker",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "ts-node": "^10.9.2",
    "typescript": "^5.7.3"
  }
}
